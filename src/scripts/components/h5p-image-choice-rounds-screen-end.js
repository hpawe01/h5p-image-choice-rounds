// Import required classes
import './h5p-image-choice-rounds-screen-end.scss';
import ImageChoiceRoundsScreen from './h5p-image-choice-rounds-screen';

/** Class representing the end screen */
export default class ImageChoiceRoundsScreenEnd extends ImageChoiceRoundsScreen {
  /**
   * @constructor
   *
   * @param {object} params Parameter from editor.
   * @param {object} [callbacks] Callbacks.
   */
  constructor(params, callbacks, contentId) {
    super(params, callbacks, contentId);
  }
}
